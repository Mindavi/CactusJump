version: '{build}'

clone_folder: c:\projects\CactusJump

# Build worker image (VM template)
image: Visual Studio 2015

configuration:
- Release
- Debug

platform:
- x64

cache:
  - build/googletest-*
  
environment:
  matrix:
  - arch: Win64

before_build:
- cmd: mkdir build && cd build && cmake --version && cmake ..
  
build:
  project: c:\projects\CactusJump\build\CactusJumpTests.sln
  parallel: true